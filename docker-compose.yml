services:
  payload:
    build: .
    ports:
      - '5252:3000'
    volumes:
      - ./media:/app/media
      - db-data:/app/media
    env_file:
      - .env
    restart: unless-stopped

volumes:
  db-data:
